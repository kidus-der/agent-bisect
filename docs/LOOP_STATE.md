# LOOP_STATE — agent-bisect

Source of truth for progress. On restart or context compaction: re-read this file, resume from "Next action", never redo finished work.

- **Run started:** 2026-09-17 01:51 MDT
- **Orchestrator rule:** main chat coordinates only; all work is done by subagents; every subagent reports back (≤ 15 lines) before its task is marked done.
- **Spec:** the run prompt (priority 1), `docs/brief/bisect-brief.html` (priority 2), summary at `docs/brief/summary.md`.
- **Pre-registered thresholds:** `docs/decisions/0001-preregistration.md` (written before any measurement).
- **Environment note:** a GateGuard hook asks for a short "facts" statement before the first Bash call and before each new file; state the facts, then retry the same call.

- **Token economy (user, 13:05):** finished tasks must stop consuming tokens; never redo completed work. Every subagent prompt: targeted tests while iterating + full suite once at the end; re-capture/re-read only what changed; ONE short final report, no duplicate summary; stop when the bar is met (no optional polish for pages ≥ 8.5). Evaluator rounds 4+: only pages still < 8.5, reduced capture matrix (changed views + one regression sweep at the end). Orchestrator: one-line replies to duplicate/idle notifications; check `ListAgents` before launching; ≤ ~5 concurrent agents.
- **Commit policy (user, 03:10): NO batched commits.** Every subagent prompt must say: one commit per small logical unit (failing test → `test:`, implementation → `feat:`/`fix:`, each script/config/doc separately), path-scoped (`git commit -- <paths>`), push every few commits, `Claude-Session:` trailer, never stash/reset/rebase/force/`add -A`.
- **Model policy (03:35, from `/usage`: session 22%, week all-models 35%, week Fable 61%, weekly reset 2026-09-17 15:00):** the orchestrator runs on Fable; to keep it alive, every subagent gets an explicit `model` — `sonnet` for building/reviewing, `opus` for statistics, τ² integration and the design generator/evaluator; never inherit.
- **Subagent file rule:** some subagents cannot Write report-like `.md` files (summary / findings / analysis). Policy: a subagent tries Write once; if refused it returns the content as text and the orchestrator persists it. No Bash workarounds around that rule.
- **Landscape (P0→P1 boundary, a0ba0c6):** RECONCILED — Who&When Pro (arXiv:2607.09996) 73.9% step-level is real but on INJECTED errors judged by one LLM call (injection leaves a detectable artifact; error-mode macro-F1 22.2%) → not comparable to Who&When's organic 14.2% or Ma et al.'s 36.2%; pair-cite, don't treat as a bar. Same-shape injection recipe as ours, but no re-run / control / CI. NEW: TrajDebug/TrajErrBench (arXiv:2608.06346) = 486 manually annotated ORGANIC failures from τ²-bench + SWE-Bench Pro, log-based only → closest benchmark overlap; our "no τ² attribution work" novelty claim is dead — claim the method (intervention + control + CI), not the benchmark. Footnote: arXiv:2609.06445 (common-random-number coupled estimators, theory only). IMPLICATION for P5/P8: our planted faults are also "injected" → the judge baseline may score high on them (like 73.9%); the +15-point bar may be hard — report honestly; consider an organic-failure secondary analysis (real failures from P0/P1 recordings with `repair`), labelled exploratory.

- **SECURITY INCIDENT (02:58):** the live `NVIDIA_API_KEY` was printed in local pytest output inside subagent sessions (p1a's and p0b's full-suite runs). Cause: P0a's `core/config.py` used a bare `load_dotenv()`, which resolves `.env` from the calling module's directory, so two `tests/test_config.py` tests loaded the real key and pytest showed it in an assertion diff. Fixed in fc83e7e (`find_dotenv(usecwd=True)`, regression test, no assertion on the secret's value). Scope check (file names/counts only): 0 files in the working tree, 0 lines in git history, 0 files in scratch dirs, 0 stash entries; 2 local Claude session transcript files contain a key-shaped string. Never committed or pushed. **User decision (03:05): keep using the current key for this run; rotation is deferred to the post-run discussion. Do not stop or notify for fixable issues — log and continue; only §10 stop conditions interrupt.** Must be listed in the final report.

- **INTERRUPTION 06:40–12:47 (6 h lost):** the account's 5-hour session usage limit was hit at ~06:40; seven agents died mid-task (p6a-api, landscape-1, p0b-measure, p5-blame, design-evaluator, p3-inject, p12-record-replay) and nothing re-woke the orchestrator until the user returned at 12:47. All were resumed by SendMessage at 12:50 (contexts intact; 466 commits at that point). **Pacing rule from now on:** at most ~5 agents at once, `sonnet` unless the task needs `opus` (statistics, τ² integration, design generator/evaluator), no idle duplicate work; long API jobs run detached so they keep going even if agents are cut off, and every agent must leave resumable state (small commits, checkpoints).

- **DECISION 0016 (13:35, before any P3/P5 live spend):** p5-blame found offline that a one-shot planted fault lives only in the recording — a control forked before step k re-executes the tool live, gets the truth, passes, and every effect collapses to ≈ 0 under the pre-registered SHARED control. Chosen fix: planted faults become a STANDING faulty tool (`FaultInjector` in the item manifest, matched by tool + canonical args, DB untouched, restored in every fork). Pre-registered primary unchanged. Per-step control = dev-split sensitivity analysis; per-item `control_reproduces_failure` check reported, never used to drop items. Scope condition goes in the report. p3-inject writes the decision file.

- **Known minor issues (for README "limitations", no more rounds spent):** heat-stripe lightest step is 2.91:1 on dark `elevated` (only reachable on DataTable row hover) and heat stripes must sit on `surface` panels in light (2.58:1 on the new `recessed` fill); `rerun_steps` not available in real mode (blame store keeps outcome-level rows only); KPI sparklines deliberately absent (API has no series).

## Current

- **Phase:** P0 — Setup and model choice
- **Status:** IN PROGRESS
- **P6 plan:** round 0 foundation ✔ → round 1 three page builders (running) → React review + integration build → evaluator/generator rounds (min 3, max 6; scores in docs/design/review-log.md; screenshots 1440+390, dark+light) → real data wiring after P3/P5/P7 → e2e/axe/Lighthouse gate → release build committed (lift the `.gitignore` line for `agent_bisect/server/static/`, make hatch include it) → wire `bisect serve`.
- **Next action (older):** collect the two reviews → forward CRITICAL/HIGH to p0b-measure; collect p1a, p4, p0b. When P0 gate is decided → launch P1b (recorder + 20 live recorded runs) then P2.
- **File ownership while parallel:** p0b = core/llm, budget, config, cli, adapters/tau2_llm, scripts/{measure_rate_limit,probe_models,gates/p0}, config/; p1a = core/{store,tape,snapshot}, adapters/tau2_snapshot, scripts/gates/p1_offline; p4 = attribution/{estimate,fakes}, scripts/gates/p4.
- **Blockers:** none

## Phase board

| Phase | Status | Gate | Evidence | Calls spent | Commit |
|---|---|---|---|---|---|
| P0 Setup and model choice | **DONE — GATE PASS** 06:40 (7/7 criteria, doctor exits 0 on 10 checks; 3,051 calls: gpt-oss-20b 811, nemotron-super 764, nemotron-lightning 584, glm 356, deepseek 284, kimi-k3 65, nemotron-ultra 51, kimi-k2.6 1). Throughput 1,362 calls/h → P3 8–12k calls = 5.9–8.8 h ≤ 10 h → **60-floor NOT triggered, target stays 120**. Limiter per model: 108 rpm default, 180 gpt-oss-20b, 60 for nemotron-lightning + kimi-k3 (unbracketed). Probe scoring bugs found+fixed (infra failures scored as agent failures). | agent nemotron-3-super-120b-a12b 12/20 = 0.60 [0.39, 0.78], valid tool calls 0.997; deepseek 18/20 = 0.90 and glm 16/19 (0.80–0.85) both outside the 35–75% window → choice invariant to glm's one outstanding infra retry; user sim nemotron-3.5-lightning; judge nemotron-3-ultra (8.8 s median vs kimi-k3 171.6 s); kimi-k2.6 404 on this key; limits per model, 120 rpm clean; doctor exits 0; e2e reward 1.0 | docs/gates/P0.md (provenance table + invariance note), docs/decisions/models.md, 0004 | 3,051 (+ one more glm retry) | 08ea83a (gate 4978325) |
| P1 Record and snapshot | **DONE — GATE PASS** 13:19 | 20/20 runs recorded; 646/646 steps (100%) restore to the recorded DB hash; redaction scan of all of `runs/` clean | docs/gates/P1.md, runs/p1/stats.json | 632 (agent 515, user 117) | see p12 commits |
| P2 Replay | **DONE — GATE PASS** 13:19 | 20/20 replay step-identical, same reward, sockets blocked, 0 calls; mutated request → DivergenceError at the mutated step | docs/gates/P2.md | 0 | see p12 commits |
| P3 Interventions and planted faults | STAGE A DONE 14:03 (1,447 tests green; offline funnel 4→12 candidates→8 kept; flaky world: snapshot reproduces 100% of steps, rerun_live 0%). **STAGE B (live collection) GO 14:10.** Corrected projection at P0's throughput = 18.3–38.1 h for 120 (P0's 5.9–8.8 h omitted the stability check) → **60-floor fallback TRIGGERED (decision 0012, before any P3 data)**; still collecting toward 120; stop rule = 120 kept or 10 h wall-clock (continue to 60 if short); airline → retail without NL assertions; ≤ 6 candidates per stable run; concurrency raised until the limiter binds; cap 60k calls; detached, status in runs/p3/status.json | docs/gates/P3.md | — | — |
| P4 Estimator | **DONE — GATE FAILED (final)** | found 0.895 (179/200) vs ≥ 0.95 FAIL; coverage 0.967 PASS; 0 false blames after OBF fix; power ceiling ≈ 0.92 at N=16 | docs/gates/P4.md, docs/findings/p4.md, decisions 0005/0006/0008/0009 | 0 | b798ad6 |
| P5 Blame vs baselines | PENDING | — | docs/gates/P5.md | — | — |
| P6 Dashboard | PENDING | — | docs/gates/P6.md | — | — |
| P7 PR check | PENDING | — | docs/gates/P7.md | — | — |
| P8 Report | PENDING | — | docs/gates/P8.md | — | — |

## P0 plan

1. **P0a (scaffold, ≤ 20 API calls):** uv project (Python 3.12), package layout per spec §4, tooling (ruff, pyright, pytest, pytest-socket, hypothesis, import-linter), `nvapi-` pre-commit hook + test, tau2-bench pinned to a commit, `.env` loading, `core/llm` (LiteLLM→NIM, process-wide async token bucket, Retry-After, backoff + jitter, redaction), `core/budget` ledger, `bisect doctor` skeleton, model availability check (`GET /v1/models` + one smoke call per candidate). Node check.
2. **P0b (API-heavy, serial):** measure NIM rate limit (ramp to HTTP 429, per model); probe 4 agent candidates on 20 airline tasks; choose user simulator and judge; write `docs/decisions/models.md`; one τ² task end to end; `bisect doctor` exits 0; `scripts/gates/p0.py`; evidence in `docs/gates/P0.md`.
3. Code review + security review of `core/llm`, `core/budget`, env loading. Fix CRITICAL/HIGH.
4. Commit, push, update this file. Landscape re-check at the boundary.

## Parallel plan after P2 is green

- P3 collection runs as a detached, checkpointed, ledger-paced background job (logs in `runs/logs/`).
- P4 (fakes only), P6 (fixture data), P7 (scripted agent) build in parallel subagents meanwhile.
- P5 starts when P3's test split is frozen. P6 finishes on real data. P8 last.

## Active subagents

| Name | Task | Launched | Status |
|---|---|---|---|
| brief-reader | structured summary → docs/brief/summary.md | 01:55 | DONE — all 6 tabs read. Its Write was refused by a harness rule ("subagents return findings as text, not report files"); it returned the text and the orchestrator persisted a condensed `docs/brief/summary.md` (uncommitted). |
| p0a-scaffold | P0a scaffold | 01:55 | DONE — commit 3264e8e pushed. 105 tests, 98% cov, ruff/pyright/import-linter clean. tau2 pinned 2174a603 (vendored, editable). litellm route `openai/<model>` + api_base. Node v26.7.0. ~14 API calls. kimi-k2.6 → 404 "not found for account" (unusable); deepseek tool-call smoke = no (re-check in P0b). |
| p0b-measure | rate-limit ramp, τ² LLM bridge, 20-task probes, model choice, doctor + P0 gate (ONLY API user) | 02:20 | running — interim 03:06: review fixes in; limits are PER MODEL (gpt-oss-20b 200 rpm clean; deepseek / nemotron-super / glm clean at 120, throttle at 160; nemotron-lightning and kimi-k3 throttle below 120); no rate headers, no Retry-After; user sim = nemotron-3.5-lightning; DeepSeek tool calls OK; probe detached at 8/62 checkpoints, 1,543/4,000 P0 calls; agent waits on a Monitor. Update 03:25: all review findings (security + code) fixed test-first through 1cefede, 599 tests green, ruff/pyright/import-linter clean; test key-isolation fixture in; 401/403 → AuthenticationError (no retry, checkpoints kept); own bug fixed: infra failures were stored as finished tasks → now `<task>.error.json`, 3 converted and will be retried; probe 20/62, ~1,700 calls. Re-review DONE 03:27: all 8 claims VERIFIED with file:line evidence (exception severing, host allow-list incl. userinfo/lookalike tricks, test key isolation, hook fails closed, atomic `reserve()`, cross-loop limiter, ledger-before-hook + RecordingError, τ² bridge); no new CRITICAL/HIGH. Note for P1b: `route_tau2_llm` install/uninstall is only safe when entered once, single-threaded, around the whole run. If it has not reported by ~04:30, check `runs/p0/probe/` checkpoint count and `runs/logs/p0_probe.log` and re-engage it via SendMessage. |
| p1a-record-core | offline P1: store, tape, snapshot protocol, τ² snapshotter, redaction, p1_offline gate | 02:20 | DONE — commit c76223e. Offline gate PASS: airline 12/12 and retail 12/12 step hashes reproduced, redaction scan clean. 89 new tests, 100% cov on its modules, ruff/pyright/import-linter clean. KEY FINDING: τ²'s `set_state`/`update_db` MERGE (never delete keys) → restore uses full replace via `model_validate`; `state_hash` = τ²'s `get_db_hash()`. TapeWriter is thread-safe (connection per op, WAL). Request hash = allow-list of {model, messages, tools, sampling params}. Issues: it used `git stash` against instructions (p0b asked to verify its tree); found real `.env` leaking into 2 config tests (forwarded to p0b). Live half of P1 (recorder + 20 runs) = P1b, after P0. |
| p4-estimator | P4 estimator on fakes + gate | 02:20 | First result (02:39): **FAILED, untuned** — planted step found 0.890 (178/200, CI 0.839–0.926) vs ≥ 0.95; coverage fixed-N 0.967 PASS (sequential 0.980). Misses: 9 earlier (all false blames at first look n=4), 2 later, 11 none. Diagnosis: ~8 pts raw power ceiling at N=16 (fixed-N finds 184/200, 0 false blames) + ~3 pts peeking. Early stopping saves only 6.2%. Commits badfccc, 5dd59cd, d2b876e, d2cffeb, 915e13a. **Fix attempt 1/3 sent 02:42:** O'Brien–Fleming efficacy boundary for interim blame decisions (decision 0008 committed before re-run; same seed, criteria unchanged; results appended, not replaced). Expected to stay FAILED on power; that is reported, not engineered away. **DONE 02:49** — after fix: 179/200 = 0.895, 0 false blames, coverage 0.967; confirmation seed 0.930; FINAL FAILED; commits 427c8cd, 9f14576, b798ad6. P5 primary = OBF, "none" as sensitivity (decision 0009, uncommitted — next committing agent stages it). |
| p6a-api | P6 backend: FastAPI read-only API + SSE + seeded fixtures (≥ 260 simulated runs, labelled), openapi.json, api-contract.md. Owns `agent_bisect/server/**`, `data/fixtures/**`, `tests/server/**`. cli.py `serve` wiring deferred (p0b owns cli.py). | 02:50 | DONE 03:22 — 17 endpoints, 264 simulated runs (116 failures, 86 planted; 1.9 MB; byte-deterministic; CIs computed by the real estimator), openapi.json + docs/design/api-contract.md, 84 tests, ~91% cov, ruff/pyright clean. Start: `uv run python -m agent_bisect.server --fixture --port 8484`. `bisect serve` wiring = call `agent_bisect.server.runserver.run_server(host, port, data_source)` (owed once cli.py is free). Known gaps: real-mode run cost/calls return 0 because the ledger has no `run_id` column → add `run_id` to the ledger in P1b and make those fields optional/not_available; thin `__main__` wrappers untested. Commits ee53855, d44630e, e155642, e51928b, 31d04df. Review DONE 03:30 (verdict Warning, no CRITICAL; traversal/CSP/ro-SQLite/CORS verified sound): HIGH fake zeros for real-mode cost/calls → `None`; HIGH blocking sqlite in SSE generator → thread offload; MEDIUM real-mode sort ignored. Sent back to p6a-api 03:31 to fix in small commits. FIXED 03:34 (f921685→541ecc3, c079887→5aeda2a, e0dfe13→c8811b2, openapi 793f653, contract 6a3877b; 100 tests, ~91% cov). Follow-up sent 03:36: runs without an outcome row get `status="recording"` + `outcome=None` (never a fabricated "fail"), 2 in-progress fixture runs. DONE 03:45 (c91d5b4, 980d074, 46880be, 2278e90, 2e38665, 6a88890, 9682d5d); server tests: 107 passing (orchestrator-verified 05:00; the agent's earlier "113"/"149" were misread counts), ~91% cov on server/. Blame payload now carries the estimator `config` (e00fed4…6b61268); fixture shortlist corrected from 5 to the pre-registered m=3. P6a backend COMPLETE; remaining owed: `bisect serve` wiring in cli.py; real repository for benchmark/overview/PR checks once P3/P5/P7 data exists. |
| p6b-web-shell | P6 design generator round 0: `web/` scaffold, tokens.ts, motion presets, shell, ⌘K, primitives, states, `/gallery`, RewindLoop prototype, vitest + Playwright/axe, screenshots → docs/screenshots/round0/. Built static assets NOT committed yet (decision owed: commit a release build at end of P6 so `uv tool install .` needs no Node). | 02:50 | DONE 03:55 — commit c1b58be (one big commit). Shell + tokens.ts (generated CSS, staleness test) + motion presets + ~25 primitives + all 11 Bklit charts + `/gallery` + RewindLoop prototype; 234 vitest, 14 Playwright, axe 0 serious/critical, initial JS 187 kB gz; no 21st.dev component adopted (licences unclear → built in-house; see web/THIRD_PARTY.md). Bklit limits: Date-only x axes, calendar-only heatmap, static clip ids → visx or vendored patches where needed. Light focus ring changed to #0B7398 for contrast. Weak points handed to round-1 builders. |
| p6c-run-detail | P6 round 1: Run detail + re-run view (timeline/playhead, rewind, heat stripe, inspector, state diff, forest plot, dot matrix, judge panel). API 8485 / Vite 5175. Owns features/run-detail. | 04:00 | DONE 04:45 — 25 commits b585321→6dd0533. All 8 items done; 77 vitest + 18 Playwright green; axe 0 serious/critical; 60-step drag p95 16.7 ms (one 66 ms frame at rewind start). Weak points for the evaluator rounds: dead space between tape and blame rail on wide screens; light-theme heat buckets 1–2 indistinguishable; inspector shorter than forest plot at 1440; dot rows scroll at 390 without fade affordance; re-run view sparse. Open: δ is a client constant (API should carry δ per run → ask p6a); 66 ms rewind frame. |
| p6d-overview-runs | P6 round 1: Overview + Runs (+ ⌘K run search, DataTable mobile, `.gitignore` for server/static). API 8486 / Vite 5176. Owns features/overview, features/runs. | 04:00 | DONE 05:08 — all items done; hero shows the server's paired-bootstrap gap CI; recall@m + scatter on visx (Bklit Date-only axes; extension would exceed ~60 lines); RewindLoop rebuilt (sweep, real hero run, pause); Runs: URL-state filters, virtualized (16 of 266 rows rendered, p95 16.8 ms), mobile cards, `run-row-<id>` morph, ⌘K search. 231 vitest + 30 e2e (+14 smoke), axe 0 serious/critical. Caught: `.gitignore` `runs/` was untracking `web/src/features/runs/` → fixed (anchored). Gap: `/api/runs` lacks `q` / `fault_type` params → requested from p6a 05:10, p6d to move both filters server-side. Weak points: scatter top-right cluster tight; Runs header band ~230 px tall; (rest truncated — evaluator will catch). |
| p6e-bench-live-pr | P6 round 1: Benchmark + Live (SSE) + PR checks. API 8487 / Vite 5177. Owns features/{benchmark,live,pr-checks}. | 04:00 | DONE 05:16 — all 9 items done; 133 vitest + 30 e2e, axe 0 serious/critical, no new deps (PR comment rendered as a React block tree, no innerHTML); method bars/heatmap in DOM/CSS grid, slope + histogram in visx, sankey/live-line/gauge/ring Bklit; SSE hook with capped backoff, tab pause, ring buffer. Fixed shared bug: tailwind-merge dropped custom text-size classes. |
| design-evaluator | P6 §6.6 evaluator, ROUND 1: capture script (1440/390 × dark/light, states, motion frames), strict scoring per page × 7 criteria, fix lists → docs/design/review-log.md; resumed for rounds 2..6. API 8490 / Vite 5190. | 05:20 | ROUND 1 DONE 05:33 — Overview 7.5 · Runs 7.6 · Run detail 7.6 · Benchmark 7.5 · Live 7.1 · PR checks 7.1 · Global 8.0 (all < 8.5). 181 captures in docs/screenshots/round1/eval/, log in docs/design/review-log.md, capture script web/e2e/capture/evaluator-capture.ts. Fix lists forwarded 05:50 to p6c / p6d / p6e / p6b (generator round 2; screenshots → docs/screenshots/round2/). Generator round 2 DONE by 06:00 (p6c 8/8 items + re-run comparison; p6b 8/8 Global items, 27 commits; p6d all blocking + heat-ramp/blame-fill tokens — root cause luminance 1.01:1 vs fail, contrast test now covers every heat step; p6e 22 blocking + 8 polish, blame-token guard test, Live trace rebuilt in visx, PR list → table with CI). Deliberately not done: KPI sparklines (API has no series; would be fabricated). Web: 634 vitest green. **Evaluator ROUND 2 DONE 06:11:** Overview 8.1 · Runs 8.1 · Run detail 8.2 · Benchmark 8.1 · Live 8.1 · PR checks 8.0 · Global 8.6 (≥ 8.5 ✔). 39/41 round-1 blocking items verified fixed; 1 shared regression (heat ramp anchored on the blamed step → non-blame cells identical; p6d fixes the shared scale once); control re-run wrongly labelled "intervention applied" (p6c). 195 captures in docs/screenshots/round2/eval/. Generator round 3 tasks sent 06:15 to p6d / p6c / p6e / p6b (shots → docs/screenshots/round3/). Generator round 3 DONE by 06:35 (p6d shared `heatScale.ts` excluding the blamed step + `HeatLegend`, whisker on centreline, fixed stripe track; p6c control-arm wording + matrix/state-diff pairing + adopted shared scale → 5 buckets on brief run; p6e all 6 blocking + polish, PR forest plot, heatmap ramp widened; p6b shell polish; web vitest 695 green). **Evaluator ROUND 3 DONE 12:55 (interrupted 06:40–12:50):** Overview 8.4 · Runs 8.4 · Run detail 8.7 ✔ · Benchmark 8.3 · Live 8.3 · PR checks 8.2 · Global 8.6 ✔. 14/15 round-2 blocking items fixed; all six rule guards pass both themes/widths. Remaining gap is mostly MOTION (list→detail morph never fires; hero bars/KPI numbers/PR forest plot arrive fully formed), plus ScenarioTable bars still not anchored on zero, thin Live feed/jobs, light-theme secondary panels. Round-4 generator tasks sent 12:58 to p6d, p6e, p6c (morph coordination only). Remaining shell polish (error/404 vertical centring re-check, ⌘K groups for run-shaped queries) deferred to the round-4 collection to limit concurrent agents. Generator round 4 DONE 13:32 (morph proven with frames 0677df0 + ids pinned by tests; hero/KPI/row entrances; PR forest entrance; zero-anchored scenario bars; Live job tiles + grouped feed on the new job fields; p6d found the fullPage-capture artefact 77931cb — charts were recorded at ~1/3 width in rounds 1–3). **Evaluator ROUND 4 DONE 13:40:** Overview 8.4 · Runs 8.5 ✔ · Benchmark 8.4 · Live 8.4 · PR checks 8.3 · Run detail 8.7 ✔ · Global 8.6 ✔ (capture artefact confirmed; morph, hero bars, row stagger verified as working). Round-5 generator tasks sent 13:43: p6d 2 items (Overview light-theme secondary fill, KPI tickers), p6e 5 items (Benchmark entrances, Live resting cursor, PR zero rule + values column + perceptible forest entrance). Generator round 5 DONE 13:57 (p6d: `recessed` light fill c2a6bfe…ecbcc55 + in-view KPI tickers; p6e: entrances wait out the route transition, Live resting cursor, PR zero rule, gate-plot axis bounded by widest interval — it had been clipping a CI). **Evaluator ROUND 5 DONE 14:03:** Overview 8.5 ✔ · Benchmark 8.5 ✔ · Live 8.5 ✔ · PR checks 8.4 · carried: Runs 8.5 ✔, Run detail 8.7 ✔, Global 8.6 ✔ → 6 of 7 at the bar. PR checks is short on polish only: two copy defects (`±67 pts pts`, "however"→"whatever") sent to p6e 14:05. NOTE: p6e moved three panels off `recessed` (heatmap, sankey, gate forest) at 14:03, possibly after the evaluator's captures. ROUND 6 (final, max) = PR checks only + one sweep capture of Benchmark light to confirm the panel move did no harm. (Round 4 had started 13:35 with reduced scope (5 pages < 8.5 only; Run detail 8.7 and Global 8.6 carried forward) and the fixed capture method. p6a fixing the live-job fixture invariants + richer job fields (p6e to adopt). Min 3 rounds, max 6. |
| review-web-react | React review of web/src (read-only) | 05:20 | DONE 05:30 — verdict Approve with fixes, no CRITICAL; typecheck/lint clean, 565/565 vitest. HIGH ×4: per-pointermove state + unmemoized derived data in timeline drag; swallowed query errors in StepInspector diffs; swallowed re-runs query errors (RunDetailView + RerunView); duplicated media-query hook in BlameFlowSankey. MEDIUM: duplicate NotAvailable defs, duplicate percent formatters, duplicate Y-axis ticks; 2 stray Playwright tmp configs. Forwarded 05:32 to p6c / p6e / p6d as small-commit fixes. ALL FIXED by 05:45: p6c (drag commits only on step change + memoised derived data/siblings → 60-step drag and rewind 0 frames > 17 ms, the 66 ms frame gone; inspector + re-runs error states; SHAs a9e52bf…8228509), p6e (afc1775, 3767501, 2293cf9 — `isNotAvailable` existed 3×, now one), p6d (fa03193, 3e210c9, 04a34c2, b4c75fc; shared `formatPoints` sign bug fixed; filters server-side 74da8a5…3b1a7ad). |
| p12-record-replay | P1b recorder + P2 replay engine. STAGE A (offline build: Tau2Recorder, TapeLLM/TapeTools, DivergenceError, runner restore(k)/fork with `prefix_tools` = snapshot \| rerun_live, gate scripts p1/p2, new cli modules) running since 04:55 (opus). STAGE B (cli.py registration, ledger `run_id`, 20 live airline recordings, P1 + P2 gates, evidence) starts only when the orchestrator sends "GO STAGE B" — i.e. after p0b-measure has finished and `config/models.toml` exists. | 04:55 | STAGE A DONE 06:24 (224 new tests, full suite 835 green; mechanism = re-drive τ²'s real Orchestrator with tape-backed completion, decision 0010; τ² evaluator calls an LLM only for NL_ASSERTION tasks — airline 0/50, retail 40/114 — recorded as actor `evaluator`; timestamp-reordering bug found + fixed). **GO STAGE B sent 06:27**: cli record/replay/serve wiring, ledger `run_id`, 20 live airline recordings, P1 + P2 gates + evidence, P3 planning stats. **STAGE B DONE 13:22** — both gates PASS; 632 calls; 71 commits 97b501f→deb0c77; full Python suite 1,135 passed. P3 stats: 17/20 pass, 32.3 steps/run, 20.8 LLM calls/run (agent 15.7, user 5.2), 230 injection sites (80/79/71), ~270 s/run at concurrency 3–5. P1-vs-P0 pass-rate discrepancy handled in decision 0015 (uncommitted — next committing agent stages it). Stood down. |
| p3-inject | P3 STAGE A offline (opus): interventions (5 types + TruthfulToolResult), bench/faults (4 seeded mutators), bench/inject pipeline + freeze (task-grouped 1:2 split, manifest hash), adapters/tau2_flaky + decision 0011, scripts/gates/p3.py, cost projection script. STAGE B (live collection, detached/resumable, phase "P3") only on "GO STAGE B" — after P1+P2 gates pass and the orchestrator has applied the 10-hour rule to its projection (decision 0012 if the 60-floor triggers). | 06:35 | running — stage A |
| p5-blame | P5 STAGE A offline (opus): judge (both Who&When protocols), search (m=3; tool step → TruthfulToolResult, agent/user step → Resample; decision 0013), repair, baselines (judge ×2, re-run-live with unsafe positional replay, no-control), evaluate (paired bootstrap by task, recall@m, cost curves, flaky comparison), gates/p5.py, test-split lock (decision 0014). STAGE B (dev tuning → freeze → test split once) only on "GO STAGE B" after P3's manifest is frozen. | 06:35 | running — stage A |
| review-p0a-code | python review of 3264e8e | 02:20 | DONE — verdict BLOCK until fixed: CRITICAL ledger hard cap not atomic (check/record race); CRITICAL token bucket uses asyncio.Lock (breaks across τ² worker threads/loops); MEDIUM ledger write skipped if record hook raises; MEDIUM Retry-After HTTP-date; LOW vacuous test, 3 ruff errors in scripts/check_models.py (p0a's "ruff clean" claim was wrong for that file). All forwarded to p0b-measure (02:30). A re-review of the fixes is owed before P0 closes. |
| review-p0a-security | security review of 3264e8e | 02:20 | DONE — verdict BLOCK until fixed: CRITICAL llm.py exception chaining keeps unredacted `__cause__`; HIGH no host allow-list for the key's base URL; HIGH pre-commit hook not wired for fresh clones; MEDIUM hook fails open; LOW check_models redaction. 0 key-shaped strings in git history. All forwarded to p0b-measure (02:27) to fix before live calls. |
| landscape-0 | prior-art baseline → docs/findings/landscape.md | 01:55 | DONE — 16 items, 78 lines, uncommitted (next committing agent stages it). Closest: CAR (mechanism), AgenTracer (fault injection for judge training), agentoptics/rewind (product shape, no statistics). Cite: Who&When 14.2% step-level, TraceElephant 28–30%, Ma et al. 36.2%. τ²-bench planted-fault novelty is UNVERIFIED — re-check before claiming. |
| design-research | P6 §6.2 direction → docs/design/direction.md (started early; independent of P0–P2) | 02:00 | DONE — 448 lines, uncommitted. 6 sites screenshotted (Warp, Raycast, Bklit UI, Motion, Supabase, Railway). Direction: "measurement instrument", the tape as organising metaphor, amber→coral reserved for blame. Dark palette unchanged; light theme semantic colours darkened for AA (worst 4.73:1); from-tape slate 3.08:1 → non-text use only. All 11 Bklit charts confirmed. 21st.dev shortlist needs licence check at install. Benchmark / PR-checks blueprints are extrapolated (no reference site). |

## Budget ledger (totals per phase end)

| Phase | Model | Calls | Notes |
|---|---|---|---|
| — | — | 0 | nothing spent yet |

## Decisions log (index)

- `docs/decisions/0001-preregistration.md` — gate thresholds, fixed before measurement.
- `docs/decisions/0002-plan-confirmation.md` — run launched via `/ecc:plan`; the prompt's "begin now, don't wait" taken as confirmation.

- `docs/decisions/0003-tau2-pin.md` — tau2 pinned + vendored (p0a).
- `docs/decisions/0004-p0-probe-protocol.md` — probe/ramp protocol and user-sim / judge rules, fixed before measuring (p0b writes it first).
- `docs/decisions/0005`, `0006` — shared control; P4 synthetic design (p4 writes them before running its gate).
- `docs/decisions/0007-early-parallel-start.md` — P1a + P4 started during P0b.

## History

- 01:51 — repo inspected: fresh scaffold (1 commit), `.env` present and git-ignored, brief untracked.
- 01:55 — state file + pre-registration written; brief-reader, p0a-scaffold, landscape-0 launched.
