# P2 gate — replay

**Verdict: PASS** at commit `bf5a1ee`.

Generated by `scripts/gates/p2.py --write-evidence`; re-running it reproduces this file from the store it just checked.

## Provenance

The commit above is repository HEAD when this ran; in a shared tree that can be an unrelated change. These are the commits that actually produced the verdict:

| File | Commit |
|---|---|
| `scripts/gates/p2.py` | dc09651 feat(gates): P2 writes its own evidence, run by run |
| `agent_bisect/adapters/tau2_replay.py` | 59e259e feat(tau2): replay and fork a recorded run through the real orchestrator |
| `agent_bisect/core/replay.py` | adc0a36 fix(replay): declare Intervention.name as a ClassVar |
| `agent_bisect/core/runner.py` | 762491d feat(runner): ForkSpec, the ForkDriver protocol and the replay checks |
| `agent_bisect/cli_replay.py` | 8479e62 feat(cli): add bisect record and bisect replay |
| `docs/decisions/0010-replay-mechanism.md` | dacc9d8 docs: record what the evaluator actually does, now it has been run |

## Gate (pre-registered in docs/decisions/0001-preregistration.md)

> 20/20 runs replay step-identical with the same reward while the network is blocked; an altered request raises `DivergenceError`.

## Commands

```
uv run python scripts/gates/p2.py --write-evidence
uv run bisect replay airline-0-t0
```

## Criteria

| Criterion | Result | Detail |
|---|---|---|
| `replay` | PASS | 20/20 runs replayed step-identically with the same reward (need 20 runs) |
| `divergence` | PASS | a mutated request at step 1 raised DivergenceError at step 1 (actor agent) |

## Runs replayed

Every response served from the tape with its request hash checked, every tool re-executed and checked against its recorded result and state hash, the whole tape consumed, and the same reward reached — with `socket.socket` replaced by something that raises for the duration.

| Run | Steps | LLM responses from tape | Reward |
|---|---|---|---|
| `airline-0-t0` | 29 | 20 | 1.0 |
| `airline-1-t0` | 25 | 15 | 1.0 |
| `airline-10-t0-a2` | 35 | 22 | 1.0 |
| `airline-11-t0` | 25 | 16 | 1.0 |
| `airline-12-t0` | 25 | 17 | 0.0 |
| `airline-13-t0` | 13 | 10 | 1.0 |
| `airline-14-t0` | 27 | 19 | 0.0 |
| `airline-15-t0` | 29 | 19 | 1.0 |
| `airline-16-t0` | 23 | 17 | 1.0 |
| `airline-17-t0` | 23 | 14 | 1.0 |
| `airline-18-t0-a2` | 103 | 54 | 1.0 |
| `airline-19-t0` | 15 | 12 | 1.0 |
| `airline-2-t0` | 57 | 38 | 1.0 |
| `airline-3-t0` | 13 | 9 | 1.0 |
| `airline-4-t0` | 25 | 17 | 1.0 |
| `airline-5-t0` | 27 | 21 | 1.0 |
| `airline-6-t0` | 63 | 41 | 1.0 |
| `airline-7-t0-a2` | 39 | 24 | 0.0 |
| `airline-8-t0` | 25 | 17 | 1.0 |
| `airline-9-t0` | 25 | 14 | 1.0 |

## Cost

0 API calls. A replay makes none, by construction.
