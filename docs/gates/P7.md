# P7 gate — the PR check

**Verdict: PASS** at commit `ce20c15`.

Generated by `scripts/gates/p7.py --write-evidence`; re-running it reproduces this file from the store it just checked.

## Provenance

The commit above is repository HEAD when this ran; in a shared tree that can be an unrelated change. These are the commits that actually produced the verdict:

| File | Commit |
|---|---|
| `demo/` | 3cdc8ed perf(demo): confirmation N=16 -> N=8 for practical PR-check wall time |
| `agent_bisect/gate/` | a7d0ea3 fix(demo): route a fork's live suffix through the recording session's router |
| `.github/` | f54742b feat(ci): the composite bisect-gate action and its PR workflow |
| `docs/decisions/0019-gate-rule.md` | 3cdc8ed perf(demo): confirmation N=16 -> N=8 for practical PR-check wall time |

## Gate (pre-registered in docs/decisions/0001-preregistration.md)

> 3 planted-regression PRs flagged with the planted step named 3/3; 3 no-op PRs give 0 false alarms.

## Commands

```
uv run python scripts/gates/p7.py --write-evidence
```

## Criteria

| Criterion | Result | Detail |
|---|---|---|
| `regressions flagged with a named step` | PASS | 3/3: demo/p7-id-slip-75=True/2, demo/p7-id-slip-90=True/2, demo/p7-id-slip-100=True/2 |
| `no-ops raise no false alarm` | PASS | 0/3 false alarms |

## Layer (a): local, 6 cases

- `demo/p7-id-slip-75` (regression): planted='use_correct_task_id.slip_probability -> 0.75', flagged=True, step=2, base=0.88, head=0.53, p=0.00262
- `demo/p7-id-slip-90` (regression): planted='use_correct_task_id.slip_probability -> 0.9', flagged=True, step=2, base=0.88, head=0.53, p=0.00262
- `demo/p7-id-slip-100` (regression): planted='use_correct_task_id.slip_probability -> 1.0', flagged=True, step=2, base=0.88, head=0.44, p=0.000229
- `demo/p7-noop-comment` (noop): planted='comment only', flagged=False, step=None, base=0.88, head=0.88, p=1
- `demo/p7-noop-rename` (noop): planted="reword a rule's prose, same id/order/slip_probability", flagged=False, step=None, base=0.88, head=0.88, p=1
- `demo/p7-noop-whitespace` (noop): planted='trailing blank line in system_prompt.md (never read at runtime)', flagged=False, step=None, base=0.88, head=0.88, p=1

## Layer (b): real PRs

NOT RUN: --skip-remote passed
