# P6 gate — dashboard: e2e, axe, Lighthouse, design score, release build

**Verdict: FAILED** at commit `f3b53e9`.

Generated by `scripts/gates/p6.py --write-evidence`; re-running it reproduces this file from the store it just checked.

## Provenance

The commit above is repository HEAD when this ran; in a shared tree that can be an unrelated change. These are the commits that actually produced the verdict:

| File | Commit |
|---|---|
| `scripts/gates/p6.py` | uncommitted |
| `web/playwright.gate.config.ts` | 2e8a9dc test(e2e): playwright config for the P6 gate against the production build |
| `web/e2e/capture/final-capture.ts` | 8e0448b test(e2e): capture script for the P6 gate's curated screenshot set |
| `docs/design/review-log.md` | f58bfb5 docs(design): round-6 final design review |
| `agent_bisect/server/static/index.html` | d71515a chore(build): release build of the dashboard |

## Gate (pre-registered in docs/decisions/0001-preregistration.md)

> Playwright e2e for every page passes; axe 0 serious/critical; Lighthouse performance >= 90 and accessibility >= 95 on Overview and Run detail; design evaluator score >= 8.5/10 on every page; screenshots in docs/screenshots/.

## Commands

```
cd web && npm run build
uv run bisect serve --fixture --port 8500
cd web && BISECT_E2E_PORT=8500 npx playwright test --config playwright.gate.config.ts
npx lighthouse http://127.0.0.1:8500/ --preset=desktop ...  (x3)
npx lighthouse http://127.0.0.1:8500/runs/brief-12-step --preset=desktop ...  (x3)
cd web && FINAL_BASE_URL=http://127.0.0.1:8500 npx playwright test --config e2e/capture/final.config.ts
uv run python scripts/gates/p6.py --write-evidence
```

## Criteria

| Criterion | Result | Detail |
|---|---|---|
| `e2e` | PASS | 101/101 passed, 0 unexpected, 0 flaky, 0 skipped |
| `axe` | PASS | 14/14 axe checks passed, 0 serious/critical violations |
| `lighthouse:overview` | PASS | performance median 94 (need >= 90), accessibility median 98 (need >= 95), n=3 |
| `lighthouse:run-detail` | PASS | performance median 95 (need >= 90), accessibility median 100 (need >= 95), n=3 |
| `design_score` | FAILED | 1/7 page(s) below 8.5: PR checks 8.4 |
| `screenshots` | PASS | all 12 curated screenshots present in docs/screenshots/final |

## Design scores (docs/design/review-log.md, round 6 final)

| Page | Score | At the bar (>= 8.5) |
|---|---|---|
| Run detail | 8.7 | yes |
| Global (shell · ⌘K · states · mobile) | 8.6 | yes |
| Overview | 8.5 | yes |
| Runs | 8.5 | yes |
| Benchmark | 8.5 | yes |
| Live | 8.5 | yes |
| PR checks | 8.4 | no |

Six of seven pages clear the 8.5 bar. PR checks is 0.1 short, held there by a 390px defect the evaluator found in round 6: `ScenarioTable` dropped every numeric column at that width, leaving a bar with no number on it. The defect was fixed afterwards (`2badc16`, `17de8f6`) but the design loop had already reached its pre-registered 6-round cap, so the fix is **not re-scored** -- reported here as a known-fixed defect on a FAILED criterion, not rounded up.

## Packaging (uv tool install ., no Node on the target machine)

Built dashboard: 1435 KiB across 117 files under `agent_bisect/server/static` (committed; see `git log -1 -- agent_bisect/server/static`), produced by `make web` (`cd web && npm run build`; `vite.config.ts`'s `build.outDir` already points there).

Verified with `uv build --wheel` that the wheel bundles it: `agent_bisect/server/static/**` is present under `agent_bisect/server/` because `[tool.hatch.build.targets.wheel] packages = ["agent_bisect"]` walks the package directory as it exists on disk, not filtered by `.gitignore` -- no pyproject.toml change was needed. `data/fixtures/` is not needed at runtime: `FixtureRepository` builds its bundle in memory from `build_bundle(seed)` (`agent_bisect/server/fixture_repository.py`), no filesystem read.

Install proof, in a clean venv (`uv venv` + `uv pip install <repo path>`, not `uv tool install` itself, to avoid touching the user's global tool list): `bisect --help` lists all commands; `bisect doctor --json` runs to completion (warns about the vendored tau2 data directory not being on `TAU2_DATA_DIR` -- a pre-existing gap outside P6's scope, unrelated to the dashboard); `bisect serve --fixture --port <port>` then answers `GET /api/health` and `GET /api/meta`, `GET /` returns the SPA's `index.html`, and a hashed asset under `/assets/` returns 200 with `etag`/`last-modified` validators. No `web/` or `node_modules/` directory exists in that venv at any point. Installing the **wheel alone** outside the source tree resolves `tau2` from PyPI instead of the vendored `vendor/tau2-bench` (`[tool.uv.sources]` only applies when uv resolves the project's own dependencies from its `pyproject.toml`) -- not what `uv tool install .` does, and out of scope for a project that is not published to a real index (docs/decisions/0003-tau2-pin.md).
