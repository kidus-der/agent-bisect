# P4 — Estimator, proven on fakes: **FAILED**

| | |
|---|---|
| **Verdict** | **FAILED** — criterion (a) missed by 6 points; criterion (b) passed |
| Date | 2026-09-17 |
| Code SHA | `d2b876e` (estimator `5dd59cd`, pre-registration `badfccc`) |
| Master seed | **20260917**, fixed in `docs/decisions/0006-p4-synthetic-design.md` before the first run |
| Network calls | zero (pure Bernoulli fakes; suite runs under `--disable-socket`) |
| Runtime | 0.23 s for 200 runs (budget was 2 min) |
| Fix attempts used | **0 of 3** — no genuine bug was found; see "Why this is not a bug" |

> **Superseded metadata, not numbers:** one fix attempt was made later (the
> O'Brien–Fleming efficacy boundary). Every number in this first section stands
> as recorded; see **Fix attempt 1** at the end of this file for the corrected
> run and the final status.

## Commands

```
uv run python scripts/gates/p4.py          # exits 1; writes runs/p4/gate.json
uv run pytest tests/attribution/ -q        # 114 tests (27 intervals, 34 estimator, 31 fakes, 22 gate)
uv run ruff check agent_bisect/attribution scripts/gates tests/attribution
uv run pyright  agent_bisect/attribution scripts/gates tests/attribution
```

## Result

```
P4 gate - estimator on synthetic runs with a planted causal step
  runs 200 - master seed 20260917 - batch 4 - max_n 16 - delta 0.1 - control shared
  planted step found    178/200 = 0.8900  95% CI [0.8391, 0.9262]
  coverage fixed N=16   3647/3771 = 0.9671
  coverage sequential   3694/3771 = 0.9796  (report only, not gated)
  mean re-runs per run  sequential 297.9 - fixed 317.7 - saving 6.2%
  mean steps tested     18.9
  misses                earlier 9 - later 2 - none 11
  -- diagnosis, report only --
  found at fixed N=16   184/200 = 0.9200  misses: earlier 0 - later 0 - none 16
  null steps flagged    sequential 10/1597 = 0.0063 - fixed 0/1597 = 0.0000
  [FAIL] planted step found: 0.8900 (requires >= 0.95)
  [PASS] coverage (fixed N=16): 0.9671 (requires in [0.92, 0.98])
  RESULT: FAIL
```

| Criterion | Required | Measured | |
|---|---|---|---|
| (a) planted step found | ≥ 0.95 | **0.8900** (178/200), 95% CI [0.8391, 0.9262] | **FAIL** |
| (b) 95% CI coverage, fixed N = 16 | in [0.92, 0.98] | **0.9671** (3647/3771) | **PASS** |

The CI on (a) excludes 0.95, so this is not a sampling accident at n = 200.

## Which coverage definition was used

Two are reported and they are **not** the same number:

- **Coverage, fixed N = 16 — the gated one.** A second pass over the same 200
  runs with `SequentialConfig(batch=16, max_n=16)`: a single look at the full
  budget, no early stopping. Coverage = the fraction of tested (run, step) pairs
  whose reported 95% interval contains the step's true effect
  `treated_prob(k) − control_prob`. **0.9671.**
- **Coverage, sequential — report only, not gated.** The same fraction for the
  interval as the sequential procedure reports it at whatever N it stopped at.
  **0.9796.**

Both passes use the same sampler seeds, so they share an identical control arm
per run and are paired. Nothing was adjusted for multiplicity; the pre-
registration says to measure the cost of peeking, not to correct for it.

Note the direction of the sequential number: it is *higher* than nominal, not
lower. Stopping early is triggered by an interval that has moved decisively away
from δ, and those stopped intervals are wide (small N), so they still tend to
cover the truth. The damage from peeking shows up in the **decision** (criterion
a), not in the interval's coverage — which is exactly why criterion (a) and
criterion (b) can disagree, and why coverage alone would have been a misleading
gate.

## Cost

| | sequential | fixed N = 16 |
|---|---|---|
| mean re-runs per run | 297.9 | 317.7 |
| saving from early stopping | **6.2%** | — |

The saving is small, and the reason is structural rather than incidental: with
δ = 0.10 and N ≤ 16, a truly-null step's interval is almost never narrow enough
to be *cleared* early. 3140 of 3771 step-tests ran the full four batches. Early
stopping pays only where a step is decisively blame-worthy, which is roughly one
step per run. Cost accounting for a shared control at ~19 tested steps is
`19 × 16 + 16 = 320` re-runs, matching the fixed column.

## Why this is FAILED and not a bug (0 of 3 fix attempts used)

The two failure modes are separable, and the fixed-N diagnosis separates them.

**1. Power, ~8 points.** At a fixed N = 16 per arm the planted step is found in
only **184/200 = 0.920** of runs, with **all 16 misses being `none`** — the
planted step's interval never cleared δ, and *no* false blame occurred
(0/1597 null steps flagged). A Newcombe interval on 16 vs 16 draws is simply
wide: a true effect of 0.53 (treated 10/16 against control 2/16) yields a lower
bound of +0.053, below δ = 0.10. The pre-registered N = 16 caps achievable
accuracy at about 92% under this generative model, before any sequential
testing is considered. **No estimator change can fix this; only a larger N
can.**

**2. Peeking, ~3 more points.** Sequential stopping turns 0 false blames into 9
(184 → 178). Every single one of those 9 was blamed at the **first look, n = 4**,
and in every one of the 9 the fixed-N design blames the correct planted step.
The per-step false-positive rate on truly-null steps is 10/1597 = **0.63%**
sequentially versus **0.00%** at fixed N. With ~8 null steps ahead of the
planted step per run, 0.63% per step ≈ 4.9% of runs lost — which matches the
observed 9/200 = 4.5%. The mechanism is exactly the one flagged in
`docs/decisions/0005-shared-control.md`: a 3/4 or 4/4 treated arm at the first
look, measured against a shared control that is often 0/16, clears δ by chance,
and the earliest-step rule then locks that false positive in. 7 of the 9 had a
shared control of 0/16 or 2/16.

**Evidence that the estimator itself is correct**, i.e. that these are
properties of the pre-registered method and not defects:

- Both intervals reproduce published reference values to 5e-5: Wilson 81/263 →
  [0.2553, 0.3662]; Newcombe method 10, 56/70 vs 48/80 → [0.0524, 0.3339]. The
  Wilson implementation is also checked against an independent derivation from
  the roots of the score equation, at 1e-12.
- Fixed-N coverage of 0.9671 is the known, slightly conservative behaviour of
  Newcombe's method 10 at small n. A miscomputed interval would not land inside
  the [0.92, 0.98] window.
- No planted step was ever stopped early as "cleared" — every `none` miss ran
  the full N = 16, so the failure is not premature stopping on the true step.
- The generator's ground truth is self-consistent: a property test asserts that
  the earliest step whose *true* effect exceeds δ is exactly the planted step in
  every generated run, so the blame rule and the label cannot disagree by
  construction.

Accordingly no threshold, δ, N, batch size, generative parameter or seed was
touched after seeing the result, and none of the three permitted bug-fix
attempts was spent. The report-only diagnosis fields (`found at fixed N=16`,
`null steps flagged`) were added to the gate script *after* the first failing
run, purely to explain it; they changed no criterion and no gated number — the
gated numbers are byte-identical before and after.

## What would have to change (a decision for P5, not a fix here)

None of these may be applied retroactively to make P4 pass. Recorded for the
phase that has to live with the consequence:

1. **Raise N.** N = 16 caps accuracy near 0.92 here. The gate's 0.95 bar and the
   pre-registered N = 16 are, under this generative model, mutually
   inconsistent — the bar was set without a power calculation.
2. **Do not act on the first look.** Requiring a minimum N before a step may be
   declared blame-worthy, or an alpha-spending boundary, would recover the ~3
   points lost to peeking at n = 4 at no extra cost.
3. **A judge shortlist helps here.** P4 tests ~19 steps per run; Bisect proper
   tests m = 3. Fewer null steps means proportionally fewer chances at a false
   early blame — so P5's accuracy on the same estimator should be *better* than
   this number, not worse, provided the judge's recall@m holds.

## Reproduce

```
uv run python scripts/gates/p4.py     # deterministic: byte-identical JSON on repeat runs
cat runs/p4/gate.json
```

---

# Fix attempt 1 (OBF efficacy boundary) — 1 of 3 used

Everything above is the **first** result and is left exactly as recorded. This
section appends the result of one fix attempt; nothing above was edited.

**What changed and why.** Acting on a nominal 95% bound at four successive looks
is not a 95% test, and under the earliest-step rule a false crossing at an early
null step can never be overturned. That is a genuine defect in the method as
pre-registered, so interim **blame** decisions now use an O'Brien–Fleming
boundary for K = 4 looks at overall two-sided α = 0.05 (z = 4.049, 2.863, 2.337,
2.024), fixed in `docs/decisions/0008-sequential-efficacy-boundary.md` and
committed (`427c8cd`) **before** the re-run. Futility stopping and the reported
nominal 95% interval are unchanged. Unchanged too: δ = 0.10, batch 4, max N 16,
the earliest-step rule, the generative parameters, the master seed and both
thresholds. Code SHA for this section: `9f14576`.

## Before / after, master seed 20260917

| | before (uncorrected) | after (OBF) |
|---|---|---|
| **(a) planted step found** | 178/200 = **0.8900**, CI [0.8391, 0.9262] | 179/200 = **0.8950**, CI [0.8448, 0.9303] |
| **(b) coverage, fixed N = 16** | **0.9671** (3647/3771) | **0.9671** (3647/3771) — *identical* |
| coverage, sequential (report-only) | 0.9796 | 0.9865 |
| **false blames** (earlier + later) | **11** (9 + 2) | **0** |
| miss breakdown earlier / later / none | 9 / 2 / 11 | **0 / 0 / 21** |
| null-step false-positive rate | 10/1597 = **0.63%** | 0/1597 = **0.00%** |
| mean re-runs per run | 297.9 | 307.4 |
| saving from early stopping | 6.2% | 3.2% |
| fixed N = 16 found (diagnostic) | 184/200 = 0.9200 | 184/200 = 0.9200 — *identical* |

Criterion (b) is identical by construction: the fixed-N pass takes a single look,
so it has no multiplicity to correct, and the reported interval never moved.

**The boundary did precisely what it was for, and it was not enough.** False
blames went to **zero** — the entire peeking failure mode is gone, on both seeds
— but the found rate rose by only 0.005. The accounting is blunt: of the 11 runs
that previously lost to a false blame, **1** now finds the planted step and
**10** now find nothing at all, because a stricter final look (z = 2.024 instead
of 1.96) is also too strict for their planted step at N = 16. Removing the false
blames converted misattributions into non-detections almost one for one.

## Confirmation seed 20260918 (declared in advance, report-only)

```
planted step found    186/200 = 0.9300  95% CI [0.8859, 0.9578]
coverage fixed N=16   3603/3692 = 0.9759
coverage sequential   3648/3692 = 0.9881
misses                earlier 0 - later 0 - none 14
null steps flagged    sequential 0/1580 = 0.0000
RESULT: FAIL
```

Same verdict, same shape: zero false blames, every miss a non-detection. The
result is not an artefact of the master seed. The gate verdict is decided on the
master seed alone; this seed is evidence only.

## Why no further attempt will be made

The remaining gap is the N = 16 power ceiling, and it is structural. Exact
enumeration over the binomials — no simulation, no seed — gives the probability
that a Newcombe lower bound clears δ = 0.10 at 16 per arm:

```
uv run python scripts/gates/p4.py --power-table
```

| control ↓ / treated → | 0.60 | 0.70 | 0.80 | 0.90 |
|---|---|---|---|---|
| 0.00 | 0.981 | 0.998 | 1.000 | 1.000 |
| 0.10 | 0.717 | 0.881 | 0.968 | 0.997 |
| 0.15 | 0.552 | 0.761 | 0.915 | 0.988 |

The generator draws the planted step's treated rate uniformly on [0.60, 0.95]
and the control rate uniformly on [0.00, 0.15], so a substantial share of runs
sit in the low-power corner of that table — at control 0.15 against treated
0.60, a correct detection is barely better than a coin flip (0.552, and 0.497 at
the OBF final look). **No estimator can be blamed for this**: it is what the
pre-registered N = 16 buys at δ = 0.10. Only a larger N, or a smaller δ, can
move it, and both are pre-registration changes that must not be made after
seeing a result.

Per the rule in `0001-preregistration.md`, no further fix attempt will be made
unless an actual bug appears. Two of the three permitted attempts remain unused.

---

## FINAL STATUS: **FAILED**

Criterion (a) planted step found = **0.8950** (179/200), 95% CI
[0.8448, 0.9303] — below the pre-registered 0.95 bar, with the interval
excluding it. Criterion (b) coverage at fixed N = 16 = **0.9671**, inside
[0.92, 0.98] — **PASS**. One of three fix attempts used; it eliminated the
peeking defect entirely (11 false blames → 0) and moved accuracy by +0.005. The
residual shortfall is the structural power ceiling of N = 16 per arm at
δ = 0.10, quantified exactly above and reported rather than engineered away. No
threshold, δ, N, batch size, generative parameter or master seed was changed at
any point after a result was seen.
