# P8 gate — report reproducibility

**Verdict: PASS** at commit `27ce091`.

Generated by `scripts/gates/p8.py --write-evidence`; re-running it reproduces this file from the store it just checked.

## Provenance

The commit above is repository HEAD when this ran; in a shared tree that can be an unrelated change. These are the commits that actually produced the verdict:

| File | Commit |
|---|---|
| `scripts/gates/p8.py` | uncommitted |
| `scripts/report/render.py` | 3b00348 feat(report): deterministic table and figure generator, and the report itself |
| `scripts/report/_offline/sitecustomize.py` | 27ce091 feat(report): make reproduce / reproduce-check, and offline enforcement |
| `Makefile` | 27ce091 feat(report): make reproduce / reproduce-check, and offline enforcement |

## Gate (pre-registered in docs/decisions/0001-preregistration.md)

> clean clone with network blocked -> make reproduce regenerates every figure/table byte-identically

## Commands

```
uv run python scripts/gates/p8.py --write-evidence
make reproduce        # the same check, against the working tree
make reproduce-check  # reproduce, with the network block enforced locally too
```

## Criteria

| Criterion | Result | Detail |
|---|---|---|
| `clone` | PASS | cloned /Volumes/main-storage-2tb/projects/agent-bisect -> /var/folders/_z/w6xp9yf54796y28q5rq79wt40000gn/T/bisect-p8-clone-avejfglt/clone |
| `environment` | PASS | `uv sync --offline` could not satisfy the environment from the cache (  cause: Distribution not found at: file:///private/var/folders/_z/w6xp9yf54796y28q5rq79wt40000gn/T/bisect-p8-clone-avejfglt/clone/vendor/tau2-bench); fell back to this repository's existing environment at /Volumes/main-storage-2tb/projects/agent-bisect/.venv via UV_PROJECT_ENVIRONMENT (documented deviation: the DATA pipeline runs offline; obtaining the interpreter itself did not, on this run) |
| `offline_render` | PASS | scripts/report/render.py exited 0 with every new network socket blocked |
| `byte_identical` | PASS | every regenerated path is byte-identical to what is committed |

## What this gate does not cover

`make reproduce-p5` (re-deriving `data/results/p5_summary.json` from the raw `runs/p5/` outcome table) is a separate, machine-local claim: `runs/` is gitignored, so no clean clone has it. This gate covers the part of P5's reproducibility claim that a clean clone actually can run -- the report layer, `scripts/report/render.py`, which reads only committed JSON under `data/`. See docs/report.md's Reproduce section.

## Regenerated paths checked

| Path |
|---|
| `docs/report.md` |
| `docs/report/` |
| `data/results/ledger_by_phase.json` |
| `data/results/p4_gate.json` |
| `data/results/p4_power_table.json` |
| `data/results/p5_power_table.json` |
| `data/results/flaky_mechanism.json` |
| `data/results/p5_summary_dev.json` |
| `data/results/p5_items_dev.json` |
